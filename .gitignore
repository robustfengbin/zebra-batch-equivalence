# Rust / Cargo
/target
**/target
Cargo.lock.bak

# cargo-fuzz run-generated data (regenerated by running the fuzzers). The real
# seed corpus lives under seeds-real/ and is committed separately.
fuzz/corpus/
fuzz/artifacts/
fuzz/coverage/
**/fuzz-*.log

# local run logs / scratch
*.log
/tmp/
scratch/

# LLVM coverage instrumentation droppings (a `cargo test` without
# LLVM_PROFILE_FILE set writes these to the repo root)
*.profraw
*.profdata

# editor / OS
.DS_Store
*.swp
