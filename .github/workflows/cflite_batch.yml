# ClusterFuzzLite: continuous (batch) fuzzing on a schedule.
#
# This is the job the M3 acceptance criterion — "CI integration green and
# self-running" — actually rests on. It is not triggered by anything a person
# does: it wakes on a timer, fuzzes every target for a bounded period starting
# from the accumulated corpus, and pushes whatever new inputs it found back to
# the corpus repository. The corpus therefore grows across runs instead of
# being rebuilt from the seed archives each time, which is what makes it a
# *regression* corpus rather than a seed set.
#
# Crashes are committed to the corpus repository's crash branch and surface as
# a failed run here.
#
# Pruning runs first, in the same workflow, because the corpus this accumulates
# is not free to carry: libFuzzer executes all of it before it mutates anything,
# and an Orchard input costs about a second. Measured 2026-09-22 on the corpus
# two days of running had produced, merging one target took 456 s to 1,644 s and
# removed 20-35% of it. Weekly was not often enough -- the first weekly prune
# timed out on its 400 s share and the job failed with eight targets unpruned --
# and pruning after fuzzing would leave the fuzzing run carrying the unpruned
# corpus.
name: ClusterFuzzLite continuous fuzzing

on:
  schedule:
    # 09:43 UTC daily. Off the top of the hour, which is when GitHub's
    # scheduled-run load peaks and scheduled runs are most often delayed.
    # Was 04:31 until 2026-09-23, when the first 04:31 slot after the schedule
    # was pushed produced no run at all -- not a failed run, no run. Changing
    # the cron line makes GitHub register the schedule afresh, and a slot the
    # same day shows whether it did.
    - cron: '43 9 * * *'
  workflow_dispatch:

permissions: read-all

jobs:
  corpus-store:
    # Decides whether this run has a permanent corpus to write to, and says so
    # out loud when it does not.
    #
    # An unset secret is substituted as the empty string, so the URL built from
    # it becomes `https://@github.com/...` and every scheduled run fails on the
    # push. The first thing a reviewer sees on this repository would then be a
    # red cross that has nothing to do with the code. The opposite default is
    # worse in a different way: quietly dropping the storage repository turns
    # the run green while the corpus it accumulates lives in the Actions cache,
    # which evicts after seven days without access -- and "permanent regression
    # corpus" is the acceptance criterion these workflows exist to satisfy.
    #
    # So: degrade, but never silently. Fuzzing still runs, the check stays
    # green, and the run states which of the two modes produced it. The
    # ordering rule in the export runbook (the secret lands before the code)
    # stays correct; this job is what makes a violation of it visible instead
    # of merely disciplined.
    runs-on: ubuntu-24.04
    outputs:
      permanent: ${{ steps.decide.outputs.permanent }}
    steps:
      - name: Is there a storage repository to write to
        id: decide
        env:
          CORPUS_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
        run: |
          if [ -n "$CORPUS_TOKEN" ]; then
            echo "permanent=true" >> "$GITHUB_OUTPUT"
            echo "Corpus store: **permanent** (storage repository)." >> "$GITHUB_STEP_SUMMARY"
          else
            echo "permanent=false" >> "$GITHUB_OUTPUT"
            echo "::warning title=Corpus is not permanent::PERSONAL_ACCESS_TOKEN is unset, so this run has no storage repository. ClusterFuzzLite falls back to the Actions cache, which evicts after seven days without access. Fuzzing still runs, but the corpus it accumulates is ephemeral and this run does not satisfy the permanent-corpus criterion."
            echo "Corpus store: **ephemeral** (Actions cache, seven-day eviction) -- \`PERSONAL_ACCESS_TOKEN\` is unset." >> "$GITHUB_STEP_SUMMARY"
          fi

  Pruning:
    # Drops inputs whose coverage the rest of the corpus already reaches, and
    # replaces the stored corpus with what is left. 2000 s a target is 20% over
    # the slowest merge measured (1,644 s for orchard_batch_composition, 699
    # inputs); unused time is redistributed to the targets still to run, so this
    # job takes as long as the merges take rather than as long as the budget.
    needs: corpus-store
    runs-on: ubuntu-24.04
    steps:
      - name: Build fuzzers
        id: build
        uses: google/clusterfuzzlite/actions/build_fuzzers@v1
        with:
          language: rust

      - name: Prune the stored corpus
        id: run
        uses: google/clusterfuzzlite/actions/run_fuzzers@v1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          fuzz-seconds: 18000
          mode: 'prune'
          storage-repo: ${{ needs.corpus-store.outputs.permanent == 'true' && format('https://{0}@github.com/robustfengbin/zebra-batch-equivalence-corpora.git', secrets.PERSONAL_ACCESS_TOKEN) || '' }}
          storage-repo-branch: main
          storage-repo-branch-coverage: gh-pages

  Fuzzing:
    # Runs whether or not pruning succeeded: a prune that times out leaves a
    # larger corpus, which is a worse run, not a reason to skip the day's
    # fuzzing. The failure stays visible on the run.
    needs: [corpus-store, Pruning]
    if: ${{ !cancelled() && needs.corpus-store.result == 'success' }}
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        sanitizer: [address]
    steps:
      - name: Build fuzzers (${{ matrix.sanitizer }})
        id: build
        uses: google/clusterfuzzlite/actions/build_fuzzers@v1
        with:
          language: rust
          sanitizer: ${{ matrix.sanitizer }}

      - name: Run fuzzers (${{ matrix.sanitizer }})
        id: run
        uses: google/clusterfuzzlite/actions/run_fuzzers@v1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          # Every fuzz target shares this budget, evenly (ClusterFuzzLite's
          # run_fuzzers.py divides fuzz-seconds by the number of targets): nine
          # targets, 1200 s each. An Orchard input drives real proof verification
          # on both paths and costs seconds, and libFuzzer executes a target's
          # stored corpus before it mutates anything -- a budget that runs out
          # during that pass ends the run with no mutation at all -- so this
          # budget is also the ceiling on how large an Orchard corpus can grow and
          # still be fuzzed past. The seed count in .clusterfuzzlite/build.sh is
          # sized against the same wall.
          #
          # Measured 2026-09-22, second run: the heaviest target loaded 546
          # stored inputs in about 550 s of its 800 s. With the Pruning job above
          # cutting the corpus back first, 1200 s leaves the larger half of the
          # budget for mutation; fuzz-health reports which half each target got.
          fuzz-seconds: 10800
          mode: 'batch'
          sanitizer: ${{ matrix.sanitizer }}
          output-sarif: false
          storage-repo: ${{ needs.corpus-store.outputs.permanent == 'true' && format('https://{0}@github.com/robustfengbin/zebra-batch-equivalence-corpora.git', secrets.PERSONAL_ACCESS_TOKEN) || '' }}
          storage-repo-branch: main
          storage-repo-branch-coverage: gh-pages

  fuzz-health:
    # Did each target fuzz, or only execute its stored corpus? A target whose
    # share of the run ends before libFuzzer finishes the corpus is logged as
    # "finished with no crashes discovered" -- the same line as a target that
    # fuzzed and found nothing -- so this reads the Fuzzing job's log and says
    # which one each target was. It warns rather than fails, like corpus-store.
    needs: Fuzzing
    if: always()
    runs-on: ubuntu-24.04
    permissions:
      actions: read
      contents: read
    steps:
      - uses: actions/checkout@v5
        with:
          sparse-checkout: |
            scripts/cflite-health.py
            fuzz/Cargo.toml
          sparse-checkout-cone-mode: false
      - name: Did every target get past its corpus
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          api="https://api.github.com/repos/${GITHUB_REPOSITORY}/actions"
          job=$(curl -fsS -H "Authorization: Bearer $GH_TOKEN" "$api/runs/${GITHUB_RUN_ID}/jobs" \
                | python3 -c 'import json,sys; print(next(j["id"] for j in json.load(sys.stdin)["jobs"] if j["name"].startswith("Fuzzing")))')
          # A job's log can lag its completion by a few seconds.
          for attempt in 1 2 3 4 5; do
            curl -fsSL -H "Authorization: Bearer $GH_TOKEN" "$api/jobs/$job/logs" -o fuzzing.log && break
            sleep 15
          done
          python3 scripts/cflite-health.py fuzzing.log fuzz/Cargo.toml
